import React, { PropTypes, Component } from 'react'
import { View, Text } from 'react-native'
import { Make } from '~/components'

export default class MakeContainer extends Component {
	render () {
		return (
			<Make />
		)
	}
}